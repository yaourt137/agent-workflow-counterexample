name: deploy-production

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - uses: actions/checkout@v4

      - name: Simulate deployment
        run: |
          echo "Deploying network policy to production"
          cat infra/prod/network-policy.json
